spring:
    datasource:
        password: ''
        url: jdbc:mysql://localhost/user_list_db
        username: root
    jpa:
        database-platform: org.hibernate.dialect.MySQLDialect
        hibernate:
            ddl-auto: create
            show-sql: true